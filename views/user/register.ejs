<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', { title: 'Register' }) %>
<body class="bg-sky-200 min-h-screen flex flex-col">
  <%- include('../partials/header') %>
  <main class="p-2">
    <div class="w-4/5 max-w-7xl mx-auto">
      <h1 class="text-2xl text-neutral-800 my-4 text-center">📝 Create an account</h2>
      <form action="/register" method="post" class="bg-sky-300 max-w-md p-4 shadow-lg mx-auto my-16">
        <div class="flex flex-col gap-4">
          <div>
            <label for="username" class="block text-neutral-800">Username</label>
            <input
              type="text"
              name="username"
              id="username"
              class="bg-sky-400 focus:bg-amber-200 rounded-sm px-2 w-full"
              value="<%= locals.values?.username || ''%>"
            >
            <% if (locals.errors?.username) { %>
              <p class="text-sm text-red-500"><%= errors.username.msg %></p>
            <% } %>
          </div>
          <div>
            <label for="email" class="block text-neutral-800">Email</label>
            <input
              type="email"
              name="email"
              id="email"
              class="bg-sky-400 focus:bg-amber-200 rounded-sm px-2 w-full"
              value="<%= locals.values?.email || ''%>"
            >
            <% if (locals.errors?.email) { %>
              <p class="text-sm text-red-500"><%= errors.email.msg %></p>
            <% } %>
          </div>
          <div>
            <label for="password" class="block text-neutral-800">Password</label>
            <input
              type="password"
              name="password"
              id="password"
              class="bg-sky-400 focus:bg-amber-200 rounded-sm px-2 w-full"
            >
            <% if (locals.errors?.password) { %>
              <p class="text-sm text-red-500"><%= errors.password.msg %></p>
            <% } %>
          </div>
          <div>
            <label for="confirm-password" class="block text-neutral-800">Confirm password</label>
            <input
              type="password"
              name="confirmPassword"
              id="confirm-password"
              class="bg-sky-400 focus:bg-amber-200 rounded-sm px-2 w-full"
            >
            <% if (locals.errors?.confirmPassword) { %>
              <p class="text-sm text-red-500"><%= errors.confirmPassword.msg %></p>
            <% } %>
          </div>
          <button type="submit" class="bg-amber-300 hover:bg-amber-400 px-4 cursor-pointer rounded-sm">
            Submit
          </button>
        </div>
      </form>
    </div>
  </main>
  <%- include('../partials/footer') %>
</body>
</html>
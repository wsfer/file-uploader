<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', { title: 'Login' }) %>
<body class="bg-sky-200">
  <%- include('../partials/header') %>
  <main class="p-2">
    <div class="flex w-4/5 max-w-7xl mx-auto">
      <form action="/login" method="post" class="bg-sky-400 mx-auto p-4">
        <div class="flex flex-col gap-4">
          <div>
            <label for="username" class="block">Username</label>
            <input
              type="text"
              name="username"
              id="username"
              class="border border-neutral-800 bg-sky-300"
              value="<%= locals.values?.username || ''%>"
            >
            <% if (locals.errors?.username) { %>
              <p><%= errors.username.msg %></p>
            <% } %>
          </div>
          <div>
            <label for="password" class="block">Password</label>
            <input
              type="password"
              name="password"
              id="password"
              class="border border-neutral-800 bg-sky-300"
            >
            <% if (locals.errors?.password) { %>
              <p><%= errors.password.msg %></p>
            <% } %>
          </div>
          <button type="submit" class="border border-neutral-800 px-2">Submit</button>
        </div>
      </form>
    </div>
  </main>
</body>
</html>